/* eslint-disable */
import React from 'react';
import { storiesOf } from '@storybook/react';
import SelectedCity from './SelectedCity';

storiesOf('SelectedCity', module).add('default', () => <SelectedCity />);
